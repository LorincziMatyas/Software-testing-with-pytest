from src.database_manager import DatabaseManager

# Create a new instance of the DatabaseManager class
# and top up the database with the initial data.

db_manager = DatabaseManager()
db_manager.topup_database()
